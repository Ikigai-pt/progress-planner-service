import Mongoose from 'mongoose';
Mongoose.Promise = global.Promise;

const mongo = Mongoose.connect(const mongo = Mongoose.connect("mongodb://mongo-0.mongo,mongo-1.mongo,mongo-2.mongo:27017/progressPlanner"));

